# 项目生命周期工作流

> 一个人同时跑很多项目时，怎么用一个「父项目」当大脑，把每个项目从想法到退役都管住。
> 这是把本仓四个 skill（[`aireadme`](aireadme/) / [`stash`](stash/) / [`pitfalls`](pitfalls/) / [`worklog-ingest`](worklog-ingest/)）串成一条工作流的方法论。它们不是孤立工具，是这条产线上的工位。

---

## 思想：为什么需要一个「父项目」

一个人同时推进十几个项目时，真正的瓶颈不是写代码，是**上下文**：每个项目的需求为什么这么定、上周那个决策的理由、三个月前在另一个项目踩过的坑，散落在脑子、笔记本、各个仓库的 commit 里，谁也串不起来。换个项目就断片，换台机器就丢失。

第一性原理上，最干净、最省力的解法是：**让一个「什么都知道」的地方当大脑。** 这个大脑本身也是一个项目，但它是所有项目的**父项目**，其他项目都是它的子项目。子项目各自闷头开发，父项目负责：把想清楚的想法孵化成项目、给新项目立项接生、每天收集所有子项目的动态、沉淀跨项目的知识。

这个父项目这里叫 **worklog**（一个个人知识库 + 工作日记系统）。它不是一个旁边的笔记工具，是整套方法的中枢。

---

## 父项目 worklog 的三重身份

worklog 同时是三样东西，这三重身份正好覆盖一个项目「从生到死」需要的全部支撑：

1. **孵化器**：新项目从这里的对话里长出来。在父项目里把需求、定位、关键决策聊透，成熟了才分出去立项。
2. **知识库（Wiki）**：所有子项目的结构化知识（架构、决策、关系、部署）沉淀在这，跨项目可查、可互相引用。
3. **日记**：每天把所有活跃子项目的动态（commit、决策、踩坑、对外动作）收集成可复盘的日记。

子项目各有自己的真相源（见下「三只手」），但它们的**诞生、每日活动、知识沉淀**全部回流到父项目 worklog。父项目是起点，也是每天的汇流口。

---

## 子项目的生命周期

一个子项目不是一条直线走到死，是个**状态机**。「暂停」才是常态，「归档」是少数。

```
外部捕获 ─深思─▶ ① 孵化 ─▶ ② 接生 ─▶ ③ 活跃 ⇄ 暂停 ──▶ 归档（少）
（任意手边工具）  （父项目     （骨架 +    每天 ingest        交付完
 零摩擦、不丢火花   对话聊透）   aireadme）  收集所有活跃子项目  / 判定没价值
        └──── 漏斗：提纯到「值得认真做」才进父项目 ────┘
```

### ⓪ 捕获（漏斗的入口，在父项目之外）

想法是突然冒出来的。第一要务是**零摩擦地接住、不丢火花**：手边任何能记的地方都行（便签、手机备忘、聊天给自己发一条）。这一步刻意不进父项目：父项目不是 inbox，是漏斗的**出口**。有空了再对着这个想法深入想一遍，想清楚「值得认真做」，才带进父项目开始孵化。

### ① 孵化（worklog 对话）

在父项目里和 AI 把需求、定位、关键决策聊透。这一步的产出是「孵化种子」：一份想清楚了的需求 + 决策 + 项目身份（名字、类型、边界）。**做工具给别人用时，这一步要追问到底**（对方现在用什么、真实痛点是什么、哪些是「不会用」而非「没有」），否则接生出来的是个自我想象的产品。

### ② 接生（aireadme）

需求稳定，就把项目分出去立项：**先铺最小骨架**（README + 一份需求文档 + 项目说明 + `git init`），不预写部署配置那些现场才定的东西。然后用 [`aireadme`](aireadme/) skill 给新项目建 `AIREADME/`：一套 AI 原生的项目文档体系，作为这个子项目自己的**真相源**（架构、决策、契约、部署各归其位，AI 和人都照它走、不跑偏）。

接生完，把新项目登记进父项目的扫描范围，从此每晚的收集会带上它。

### ③ 活跃 ⇄ 暂停（开发主循环）

开发期间：写代码、用 [`stash`](stash/) 把项目级的决策和踩坑存进项目记忆、架构变了就用 aireadme 更新真相源。

关键是**「暂停」是一等状态**，不是失败。自己的项目忙不过来时，不强行推进、也不杀掉，就**暂停**，有空再续。它仍是活的，只是这段时间没动静。这反直觉但很实用：逼自己「要么做完要么放弃」，反而会因为压力做出更差的取舍。

### ④ 每晚收尾（worklog-ingest）

每天收尾时用 [`worklog-ingest`](worklog-ingest/) skill，跨**所有**活跃子项目把这一天聚合成日记：扫各项目 commit、收集决策与踩坑、更新 Wiki、提交（有进展的晚上才有日记可聚，没动静的夜晚自然跳过）。这是 N 条并行的子项目生命周期每晚的**汇流口**：不管白天在几个项目间跳，晚上都回到父项目沉淀成一条可复盘的记录。

### ⑤ 归档（少数）

终点有两种：**客户项目交付即结束**；**自己的项目判定不再有价值才主动结束**。这两种走归档（Wiki 标记 + 移出活跃区）。因为「忙不过来」只会进暂停、不会进归档，所以真正归档的项目并不多。

---

## 三只手：三个尺度的真相源

父项目伸向子项目的三只手，各自维护一个**不同尺度**的真相源。这是整套方法在工程上的核心：**文档与记忆不是一锅烩，按尺度分层治理。**

| skill | 尺度 | 真相源在哪 | 管什么 |
|---|---|---|---|
| [`aireadme`](aireadme/) | **项目尺度** | 每个子项目仓自己的 `AIREADME/` | 这个项目的架构、决策、契约、部署 |
| [`worklog-ingest`](worklog-ingest/) | **活动尺度** | 父项目 worklog 的日记 + Wiki | 跨所有项目的每日动态、长期知识 |
| [`stash`](stash/) | **会话尺度** | Claude Code 的项目记忆目录 | 一段对话里值得跨会话留存的决策、偏好、坑 |

三者不重叠：项目内的架构契约归 aireadme、跨项目的活动归 worklog、会话里的临时学习（含踩坑）归 stash。一条信息只进一个尺度。

更准地说，它们落在两条正交轴上：**跨项目 vs 项目本地**（只有 worklog 跨项目，aireadme 与 stash 都在项目本地）+ **策展 vs 原始**（aireadme 是策展后的项目真相，stash 是会话里的原始捕获）。所以晋级是有方向的：会话里反复出现、稳定下来的决策，从 stash 毕业进 aireadme 的决策记录；跨项目反复用到的，再回流 worklog Wiki。这和下面坑的提纯阶梯是同一套机制。

---

## 集体记忆：一个项目踩坑，整个舰队免疫

父项目结构带来的最大杠杆，不是孵化能力，是**集体记忆**。

默认情况下，记忆是**项目隔离**的：A 项目里学到的工程教训，B 项目的 session 看不见。于是同一个 macOS locale 坑、同一个 git 坑、同一个部署坑，被不同项目轮流踩一遍。单人跑十几个项目，这是巨大的隐性浪费。

解法是给坑分三层、设一条**提纯阶梯**：

| 层 | 是什么 | 存哪 | 谁投递 |
|---|---|---|---|
| **单项目坑** | 只对单个项目成立 | 该项目的会话尺度记忆（[`stash`](stash/) 写，aireadme 管架构契约、不管踩坑） | 项目内 session 自动加载 |
| **通用工程坑** | 任何项目都可能踩 | [`pitfalls`](pitfalls/) 的库 | 干高危活前主动查 |
| **全局铁律** | 最高频最致命的少数 | 全局指令（每个 session 自动加载） | 永远在场 |

一个项目踩到坑，先判断「单项目还是通用」。通用的就抽象出来升进 [`pitfalls`](pitfalls/) 库；之后任何项目动同类活之前查一眼，就**对这个坑免疫了**：不用每个项目各踩一遍。被反复查到、最致命的那几条，再蒸馏一句进全局铁律，变成永远在场的护栏。

举个具体的：一个 macOS grep 默认 locale 漏中文匹配的坑，最早只记在某个项目的记忆里；抽象进 [`pitfalls`](pitfalls/) 后，后来的项目在动 grep 之前一查就避开了，没有第二个项目再踩同一个。这就是「舰队免疫」：把单个项目用血换来的教训，变成所有项目的共享抗体。父项目 = 集体记忆的载体，这条阶梯 = 抗体的投递机制。（严格说，中间层不是被动免疫，是主动接种：查一次 = 打一针；只有蒸馏进全局铁律的那几条，才是永远在场的被动免疫。）

> ⚠️ 有个硬约束值得说破：把坑「存在某个项目里」并不会让别的项目自动看见，记忆是项目隔离的。所以共享必须靠跨项目的投递通道（可主动查的 [`pitfalls`](pitfalls/) 库 + 每个 session 都加载的全局指令），而不是指望「存下来就生效」。

---

## 小结

这套工作流的内核是一句话：**用一个父项目当大脑，把一群项目的生老病死管起来。**

- 父项目（worklog）是中枢：孵化器 + Wiki + 日记。
- 子项目走一条状态机：捕获 → 孵化 → 接生 → 活跃⇄暂停 → 归档。
- 三只手按尺度分层维护真相源：aireadme（项目）/ worklog-ingest（活动）/ stash（会话）。
- 集体记忆让一个项目的教训变成整个舰队的免疫力。

四个 skill 都开源在本仓（其中 worklog-ingest 含个人 hardcode、主要作参考，[`aireadme`](aireadme/) / [`stash`](stash/) / [`pitfalls`](pitfalls/) 可直接复用；本仓另有的 story-writer 是独立的内容创作 skill、不属于这条工作流）。

**最小上手**：装 aireadme + stash + pitfalls，建一个笔记仓当你的父项目，给 pitfalls 配上那条全局纪律，就能开始跑这条线。合起来，这是一个人把很多项目跑得不丢上下文的一套方法。
